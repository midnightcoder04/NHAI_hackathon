# Expo version

This app is pinned to **Expo SDK 55** (see `mobile/package.json`). Read the exact
versioned docs at https://docs.expo.dev/versions/v55.0.0/ before writing any code.
Do NOT install SDK 56 packages — a stray `@expo/dom-webview@56` previously caused a
native white-screen crash on startup.
